#include<stdio.h> 
  
int fib(int n) 
{ 
   if (n <= 1) 
      return n; 
   return fib(n-1) + fib(n-2); 
} 
  

int main () 
{ 
  int s;
  scanf("%d",&s);
  printf("Number of ways = %d", fib(s+1)); 
  
  return 0; 
}
